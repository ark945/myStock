# myStock 股票追蹤儀表板 — 系統操作手冊

---

## 1. 系統簡介與核心功能

### 1.1 系統簡介
`myStock` 是一套專為台股與美股投資人打造的輕量化、現代化個人股票即時追蹤儀表板。具備自選清單分類、即時股價輪詢、損益試算、大盤指數監控、五檔買賣價量視覺化以及基本面財務指標分析等完整功能。後端採用 Python FastAPI，資料持久化儲存於 Supabase PostgreSQL 資料庫，支援本地端、Docker 容器化以及 GitHub 連動之高可用雲端部署。

### 1.2 核心功能
1. **多市場行情即時監控**：
   - **台股市場**：整合 `twstock` 與證交所即時行情，支援股票代號/名稱即時模糊搜尋、最佳五檔價量、開高低收現價與當日漲跌幅。
   - **美股市場**：整合 `yfinance`，支援美股主要代碼查詢、盤後交易數據與即時跳動。
   - **大盤與指數期貨**：加權指數、櫃買指數、台指期貨即時價差與走勢監控。
2. **多清單與自選股管理 (Watchlist)**：
   - 支援多使用者與多自選分類清單（新增、重命名、刪除、自選股拖曳排序）。
   - 損益試算器：支援輸入買入成本價與股數，自動計算未實現損益、損益率與報酬金額。
3. **基本面與財報數據深度分析**：
   - 本益比 (P/E Ratio)、股價淨值比 (P/B)、殖利率 (Dividend Yield)、ROE 股東權益報酬率、Beta 波動係數、50日/200日均線與 52 週高低價區間。
4. **高穩定多平台相容架構**：
   - 支援動態環境變數 `PORT`，可相容 Render、Zeabur、Fly.io、Hugging Face Spaces 與本機 Docker。
   - 支援跨來源資源共用 (CORS)，方便後續前後端分離或 GitHub Pages 呼叫。
   - 提供 `/api/health` 監控端點，搭配保活服務實現 24/7 不中斷運作。

---

## 2. 專案目錄架構與模組清單

```text
myStock/
├── .ai_rules.md              # AI 協同開發與規範設定
├── Dockerfile                # 多平台相容容器映像檔建置設定
├── render.yaml               # Render.com 藍圖配置檔 (一鍵自動設定新加坡節點與環境變數)
├── README.md                 # 專案公開說明文件
├── 操作手冊.md               # 本系統完整操作與維運指南
├── requirements.txt          # Python 依賴清單
├── config.json               # 本地端金鑰與資料庫配置檔 (已 gitignore)
├── supabase_init.sql         # Supabase 資料庫資料表與索引初始化腳本
├── app.py                    # FastAPI 核心應用主程式 (API 路由、定時更新排程、CORS)
├── stock_service.py          # 台美股爬蟲、yfinance/twstock 資料清洗與快取模組
├── static/                   # 靜態前端資源
│   ├── index.html            # 儀表板前端主結構
│   ├── style.css             # 深色科技風自定義 CSS 樣式
│   └── app.js                # 前端互動邏輯、即時輪詢、繪圖與 API 溝通
└── docs/                     # 專案文件與螢幕截圖
    └── ai_planning/          # AI 架構規劃與遷移評估文件
        └── github_deployment_plan.md
```

### 模組說明
- **`app.py`**：FastAPI 伺服器，負責監聽請求、維護每 60 秒的背景價格更新迴圈 (`price_updater_loop`)、提供健康檢查端點 `/api/health`，並兼具靜態網頁派送服務。
- **`stock_service.py`**：核心報價引擎。整合 Yahoo Finance (yfinance)、證交所即時行情與本地記憶體快取機制，提供重試、降級處理與開盤時段判定。
- **`supabase_init.sql`**：定義 `user_profiles`、`watchlists`、`watchlist`（自選股明細）等資料表結構、外鍵級聯與 RLS 存取政策。

---

## 3. 環境建置與依賴安裝步驟

### 3.1 本地 Python 環境需求
- **作業系統**：Windows 10/11、macOS 或 Linux
- **Python 版本**：建議 **Python 3.10 或 3.11**

### 3.2 安裝步驟
在專案根目錄開啟終端機（PowerShell / Bash）：

```powershell
# 1. 建議建立虛擬環境 (選擇性)
python -m venv venv
.\venv\Scripts\Activate.ps1   # Windows PowerShell
# source venv/bin/activate    # Linux / macOS

# 2. 安裝核心依賴模組
pip install -r requirements.txt
```

依賴模組主要包含：
- `fastapi` & `uvicorn[standard]`：高效能非同步 Web 框架與伺服器
- `yfinance`：Yahoo Finance 美股與全域行情套件
- `twstock`：台灣證券交易所股票資料介面
- `supabase`：Supabase 官方 Python SDK
- `curl_cffi`：高效能 HTTP 請求客戶端

---

## 4. 組態設定說明 (Config & Settings)

本系統支援「本機設定檔 (`config.json`)」與「雲端環境變數 (Environment Variables)」雙軌運作：

### 4.1 本地端設定檔：`config.json`
在專案目錄下新增或確認 `config.json`（此檔案已被 `.gitignore` 排除，避免外洩）：
```json
{
  "supabase_settings": {
    "url": "https://<YOUR_PROJECT_ID>.supabase.co",
    "key": "<YOUR_SUPABASE_SERVICE_ROLE_OR_ANON_KEY>"
  }
}
```

### 4.2 雲端環境變數 (Environment Variables)
在雲端容器或託管平台（Render、Zeabur、HF Spaces）中，系統會自動優先讀取以下系統環境變數：

| 環境變數名稱 | 必填 | 說明 | 範例值 |
| :--- | :---: | :--- | :--- |
| `SUPABASE_URL` | 是 | Supabase 專案 API URL | `https://xxxx.supabase.co` |
| `SUPABASE_KEY` | 是 | Supabase 專案 API Key (`service_role` 或 `anon`) | `eyJhbGciOi...` |
| `PORT` | 否 | 伺服器監聽通訊埠 (平台自動注入，預設 7860) | `10000` 或 `8000` |

---

## 5. 操作指令與使用範例

### 5.1 本地啟動伺服器
```powershell
# 方式 A：使用 uvicorn 指令
uvicorn app:app --host 0.0.0.0 --port 7860 --reload

# 方式 B：指定 Python 模組啟動
python -m uvicorn app:app --host 127.0.0.1 --port 7860
```
啟動後打開瀏覽器訪問：`http://127.0.0.1:7860/?user=Ark` 即可進入儀表板。

### 5.2 使用 Docker 本地運行
```powershell
# 1. 建置 Docker 映像檔
docker build -t mystock:latest .

# 2. 啟動容器 (將容器 7860 映射至本機 7860)
docker run -d -p 7860:7860 --name mystock_app mystock:latest
```

---

## 6. 雲端部署指引 (由 HF 遷移至高穩定度平台)

### 方案 A：GitHub 連動 Render.com 免費自動部署 (推薦)

Render 是目前與 GitHub 連動最順暢、線路極穩的雲端服務，每次推送到 GitHub 即自動建置上線：

#### 步驟 1：推送程式碼至 GitHub
```powershell
git add .
git commit -m "feat: enhance deployment compatibility for Render and GitHub"
git push origin main
```

#### 步驟 2：在 Render 建立 Web Service
1. 瀏覽 [Render.com](https://render.com/)，使用 **GitHub 帳號** 授權登入。
2. 點擊右上角 **"New +" -> "Web Service"**。
3. 選擇 **"Build and deploy from a Git repository"**，選中您的 `ark945/myStock` 倉庫。
4. 填寫部署組態：
   - **Name**：`mystock`
   - **Region**：建議選擇 `Singapore`（新加坡節點對台灣連線延遲最低）
   - **Branch**：`main`
   - **Runtime**：選擇 **`Docker`**（系統會自動識別專案內的 `Dockerfile`）
   - **Instance Type**：選擇 **`Free`**
5. 展開 **"Environment Variables"**，加入兩組環境變數：
   - `SUPABASE_URL` = 您的 Supabase URL
   - `SUPABASE_KEY` = 您的 Supabase Key
6. 點擊 **"Create Web Service"**。Render 會開始自動執行 Docker build，約 2-3 分鐘後即可取得專屬安全網址（例如 `https://mystock-xxxx.onrender.com`）。

#### 步驟 3：防休眠保活 (Keep-Alive)
Render 免費方案在 15 分鐘無人存取會進入睡眠。若要達成 24 小時秒開：
1. 前往免費服務 [UptimeRobot.com](https://uptimerobot.com/) 或 [cron-job.org](https://cron-job.org/)。
2. 新增監控點（Monitor），類型選 **HTTP(s)**。
3. 監控網址填入：`https://您的Render網址/api/health`。
4. 檢查頻率設定為：每 10 分鐘一次。
5. 即可確保後台定時排程與前端頁面永遠保持秒開，絕不受休眠影響。

---

### 方案 B：GitHub 連動 Zeabur 部署 (亞太台灣極速節點)
1. 前往 [Zeabur.com](https://zeabur.com/) 並以 GitHub 登入。
2. 建立新專案，點選 **"Deploy New Service" -> "Git"**，選擇 `ark945/myStock`。
3. Zeabur 會自動分析並採用 Docker 部署。
4. 在服務的「Variables」分頁填入 `SUPABASE_URL` 與 `SUPABASE_KEY`。
5. 在「Networking」分頁一鍵綁定免費網址（例如 `mystock.zeabur.app`）。

---

### 方案 C：本機 NUC + Cloudflare Tunnel (100% 免費自架私有雲)
使用者本機環境為 NUC 主機，若希望 100% 掌握在自己手裡且零額度限制：
1. 下載並安裝 [Cloudflare cloudflared](https://developers.cloudflare.com/cloudflare-one/connections/connect-networks/downloads/)。
2. 終端機執行快速穿透：
   ```powershell
   cloudflared tunnel --url http://localhost:7860
   ```
3. 終端機將立即輸出一個全球可用的安全 HTTPS 網址（例如 `https://random-name.trycloudflare.com`），直通您的 NUC 伺服器。

---

## 7. 輸出結果與報表解讀

1. **頂部市場概況卡片**：
   - **台股加權指數 / 櫃買指數**：顯示即時指數數值、漲跌點數與百分比。開盤時間即時跳動，盤後顯示收盤資訊。
   - **美股主要指數**：S&P 500、那斯達克、道瓊指數即時行情。
2. **股票自選清單表格**：
   - **標的資訊**：股票代碼、公司中文名稱、市場標記（上市/上櫃/美股）。
   - **即時價格**：綠底（跌）/ 紅底（漲），符合台灣股市顏色習慣。
   - **五檔即時量價**：展開卡片可檢視買一至買五、賣一至賣五委託掛牌張數。
   - **財務健全指標**：P/E < 15 標註估值便宜；ROE > 15% 標註高獲利能力；殖利率 > 5% 標註高息股。
3. **未實現損益卡片**：
   - 彙總計算所有持股之買入總市值、現行總市值、未實現損益總額與整體投報率 (%)。

---

## 8. 常見問題排解 (FAQ & Troubleshooting)

### Q1：股票價格顯示為「--」或抓取失敗？
- **原因**：台股盤後 API 或 Yahoo Finance 暫時限流，或是該股票代號輸入有誤（例如興櫃股票代號不同）。
- **解法**：
  1. 檢查代號是否正確（台股輸入 4 碼，如 `2330`；美股輸入英文代碼，如 `NVDA`）。
  2. 檢查伺服器主控台日誌，系統具備自動降級重試機制，通常於 10~30 秒內自動回復。

### Q2：更新自選股後重新整理頁面消失？
- **原因**：Supabase 資料庫連線異常或未完成初始化 SQL。
- **解法**：
  1. 檢查 `config.json` 或雲端環境變數 `SUPABASE_URL` 與 `SUPABASE_KEY` 是否正確填寫。
  2. 登入 Supabase SQL Editor，確認已執行 `supabase_init.sql`，並確認 `watchlist` 與 `user_profiles` 表已成功建立。

### Q3：從 Hugging Face 遷移至 Render 後，資料庫的自選股資料會保留嗎？
- **會**。所有自選清單、損益成本與使用者設定均儲存於雲端的 Supabase PostgreSQL，不受前後端主機更換影響。只要新的 Render 服務設定相同的 `SUPABASE_URL` 與 `SUPABASE_KEY`，上線後即無縫接軌所有現有資料。

### Q4：推送至 GitHub 時出現 `rejected (non-fast-forward)`？
- **原因**：遠端 GitHub 倉庫已有本地未同步的變更。
- **解法**：
  ```powershell
  git pull origin main --rebase
  git push origin main
  ```
